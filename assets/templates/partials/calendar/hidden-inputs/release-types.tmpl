{{ range $type, $value := .ReleaseTypes }}
  {{ if and  $value.IsChecked (ne $value.Name "census") }}
  <input
    type="hidden"
    name="{{ $value.Name }}"
    value="{{ $value.Value }}"
  >
  {{ end }}
{{ end }}
